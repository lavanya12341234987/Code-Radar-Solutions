#include <stdio.h>

int main() {
    char ch;
    // Prompt the user to enter a character
    printf("Enter a single character: ");
    scanf("%c", &ch);
    printf("You entered: %c\n", ch);

    return 0;
}
    
